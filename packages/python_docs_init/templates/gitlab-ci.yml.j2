pages:
  stage: deploy
  image: ghcr.io/astral-sh/uv:python3.11
  script:
    - uvx python-docs-init build . --strict
    - mv site public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
